# Google Cloud Configuration
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
GOOGLE_CLOUD_LOCATION=global

# Optional GCS bucket for Agent Runtime artifact storage / logging
LOGS_BUCKET_NAME=your-gcs-bucket-name
